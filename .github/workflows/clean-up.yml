name: Cleanup Actions history

on:
  schedule:
    - cron: '0 8 * * 0'
  workflow_dispatch:

permissions:
  actions: write
  contents: read

jobs:
  cleanup:
    uses: CassioCintra/platform-ops/.github/workflows/reusable-actions-cleanup.yml@main
    with:
      keep-runs: 5
